# リモートデプロイ設定ガイド

`.github/workflows/deploy.yml` は `main` へのpushでNode 20を用い、`npm ci`、build、typecheck、test、production D1 migration、Worker、Pagesの順に実行します。merge前に [`docs/realtime_authorization_setup.md`](./docs/realtime_authorization_setup.md) のチェックリストを完了してください。

## GitHub Actions Secrets

Repositoryの **Settings → Secrets and variables → Actions** に登録します。

| Name | 用途 |
| --- | --- |
| `CLOUDFLARE_API_TOKEN` | D1/Workers/Pagesをdeployできる最小権限token |
| `CLOUDFLARE_ACCOUNT_ID` | 対象Cloudflare account |
| `VITE_API_URL` | production Worker base URL |
| `SUPABASE_URL` | Keep Alive対象の単一Project URL |
| `SUPABASE_ANON_KEY` | Keep Alive用の公開legacy anon key |

値をworkflow、Markdown、Issueへ直書きしません。Cloudflare tokenは一度しか表示されないため、安全なCredential管理先へ保存します。

## Cloudflare Worker設定

次をproduction Workerへ設定します。`JWT_SECRET`、`SUPABASE_JWT_SECRET`、`SUPABASE_SERVICE_ROLE_KEY`、初回passwordはSecretとして扱います。`SUPABASE_URL` と `ALLOWED_ORIGINS` は秘密ではありませんが、環境別設定として管理します。

- 常時: `JWT_SECRET`
- 常時: `SUPABASE_JWT_SECRET`
- 常時: `SUPABASE_URL`
- 常時: `SUPABASE_SERVICE_ROLE_KEY`
- 常時: `ALLOWED_ORIGINS`
- Teacherが0件の初回のみ: `INITIAL_TEACHER_USERNAME` / `INITIAL_TEACHER_PASSWORD`

`ENVIRONMENT=production` は `packages/backend/wrangler.toml` に設定済みです。bindingが欠落・未知値でもWorkerはfail closedとし、開発fallbackを使いません。

`packages/backend` から設定する例:

```bash
npx wrangler secret put JWT_SECRET
npx wrangler secret put SUPABASE_JWT_SECRET
npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY
```

コマンド入力値やDashboard値をshell history・ログ・commitへ残さない運用を行います。初回Teacher作成を確認後、bootstrap credentialsは削除可能です。

## Supabase外部作業

単一Projectへ `supabase/migrations/202608300001_realtime_authorization.sql` を適用し、Realtime Settingsの **Allow public access** をOFFにします。Roomには同じProject URLと公開anon keyだけを保存します。詳細は[Realtime Authorization設定](./docs/realtime_authorization_setup.md)を参照してください。

## Workflow確認

GitHubの **Actions → Deploy All → 対象run → 最初の失敗step** を確認します。Keep Aliveは **Actions → Supabase Keep Alive → 対象run → Send keep-alive request to Supabase REST API** を確認します。エラーコードと切り分けは[Runbook](./docs/troubleshooting.md)を参照してください。

Cloudflare Free + Supabase Freeで構成でき、Repository側に課金・自動Plan移行・クレジットカード登録を要求する処理はありません。利用量は各社の最新limitsを確認してください。
